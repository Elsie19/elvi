program = { SOI ~ ((WHITE_SPACE+)? ~ statement ~ (newline)*)+ ~ EOI }

newline = _{ ";"{1} | NEWLINE }

COMMENT = _{ (WHITE_SPACE+)? ~ "#" ~ (!NEWLINE ~ ANY)* ~ NEWLINE }

statement = {
    (WHITE_SPACE+)? ~ (normalVariable | readonlyVariable | builtinDbg // | externalCommand
  | ifStatement)
}
