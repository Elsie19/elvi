program = { SOI ~ (statement ~ (newline)*)+ ~ EOI }

newline = _{ ";"{1} | NEWLINE }

COMMENT = _{ (WHITE_SPACE+)? ~ "#" ~ (!NEWLINE ~ ANY)* ~ NEWLINE }

statement = {
    normalVariable
  | readonlyVariable
}
