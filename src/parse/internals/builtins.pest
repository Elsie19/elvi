// Main grammars
builtinDbg   = !{ "dbg" ~ (" " | "\t")+ ~ variableIdent }
builtinUnset = !{ "unset" ~ (" " | "\t")+ ~ variableIdent }
builtinExit  = !{ "exit" ~ ((" " | "\t")+ ~ (anyString | elviNumber){1})? }

// Wrapper grammar for any builtin
builtinWrapper = { builtinDbg | builtinUnset | builtinExit }
