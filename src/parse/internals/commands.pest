// Main rules
externalCommand = @{ externalCommandFirstWord ~ externalCommandRest* }

externalCommandFirstWord = { (ASCII_ALPHA | "_" | "-" | ASCII_DIGIT)+ }
externalCommandRest      = { (!(newline) ~ ANY)+ }
