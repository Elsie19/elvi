// Main rules
ifStatement = { "if " ~ ifStatementMatch ~ newline+ ~ (space+)? ~ "then" ~ newline+ ~ #then_block = (statement+) ~ newline+ ~ "fi" }

ifStatementMatch = ${
    backtickSubstitution
  | builtinWrapper
}
